import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.skyway.stuntdriver',
  appName: 'Skyway Stunt Driver',
  webDir: 'dist'
};

export default config;
